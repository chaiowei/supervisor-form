# 更新紀錄

## 2026-07-26

### Gemini 模型改用 latest alias
- `Translate to ZH1` 節點的 Gemini 模型從釘死版本號（`gemini-2.5-flash`）改用 `gemini-flash-latest` alias。原因：Google 已兩度下架特定版本號（先前 `gemini-2.0-flash` 也發生過），導致 workflow 中斷；下架後的錯誤訊息會被 n8n 包裝成看起來像「JSON parameter needs to be valid JSON」的語法錯誤，難以第一時間判斷根因。改用 alias 可避免未來再因版本號被下架而中斷。(`11bdf8a`)

## 2026-07-24

### 翻譯語意修正
- 改進 `Translate to ZH` 節點的 Gemini prompt，修正翻譯常見的語意錯誤：數字被錯誤合併（如「1台+5人」變成「15輛」）、列表項目被拆成無意義片段句、缺分隔符的短語被字面堆疊等問題。新增規則要求保留列表結構、數字原樣不重組、缺分隔符時補頓號、句意需完整，輸出更接近工程師手寫的自然中文而非逐字直譯。(`98974c9`)

## 2026-07-14

### 工程名稱改多選、新增工程師/廠商/工程三層權限篩選
- 工程名稱欄位從單選下拉改為彈出式多選勾選視窗，送出時合併成同一個 `projectName` 欄位（逗號分隔），n8n/PDF/LINE 訊息端不需修改。
- 新增連動篩選：選「負責工程師」→ 廠商下拉只顯示該工程師底下的廠商 → 選廠商 → 工程名稱清單只顯示該廠商底下的工程；切換上層選項時自動清空並停用尚未符合條件的下層欄位。
- 草稿暫存/還原邏輯同步支援連動重建與多選項目還原。
- Google Sheets 選項來源新增「所屬上層」欄位，補上工程師/廠商/工程名稱的從屬關係資料。(`c2538cf`)

### 選單載入時序修復
- 修復「負責工程師」下拉選單資料尚未載入完成前就可提前選取，導致廠商/工程清單顯示為空的問題（與選哪位工程師無關，是單純的載入時序問題）。改為 CSV 選項資料載入完成前鎖住「負責工程師」選單並顯示「載入中…」，載入完成才開放選取，從根本排除這個時間窗口。(`6444a05`)

## 2026-07-10

### PDF 轉檔效能優化
- PDF.co 轉檔流程改用 async + 輪詢（`Wait Job` / `Check Job` / `Job Poll Gate`）搭配 `Split Out`，讓承包商語言版與中文版 PDF 真正同時送出與查詢，不再是兩條各自序列處理的分支，改善整體送出等待時間過長的問題。(`e5cb4ac`)

## 2026-07-06

### PDF 表頭調整
- 中文版 PDF 表頭改回「監工日誌」（`Generate PDF HTML` 節點 `LBL.zh.title`）。英文/泰文版標題與網頁表單本身的頁面標題不受影響。(`969232f`)

### Gemini 升級與翻譯邏輯調整
- `Translate to ZH` 節點模型從 `gemini-2.0-flash` 升級為 `gemini-2.5-flash`。
- **中文表單完全跳過翻譯**：新增 `Is ZH Source?` 判斷節點，`language==='zh'` 時直接繞過 Gemini API 呼叫，避免模型誤改已經正確的中文內容。
- **英文/泰文表單強制完整翻譯**：加強 Gemini prompt，明確要求「輸入可能泰英中混雜，所有非中文部分都要翻完，不可以只翻一部分」。(`c1c60f2`, `33d1a6a`)

### 顯示文字調整
- 所有面向使用者的「監工人員」字樣改為「負責工程師」：網頁表單標題/下拉選單標籤/錯誤訊息/選項載入失敗提示、PDF 欄位標籤、email 通知內文。
- 保留不變：Google Sheets CSV 的 `type` 欄位比對值仍是「監工人員」（資料結構定義，非顯示文字，Sheet 端也維持原樣）。(`33d1a6a`)

### PDF 新增產出時間戳
- PDF 右下角新增淡灰色（`#aaaaaa`）8pt 時間戳，格式 `YYYY-MM-DD HH:mm`，使用 `position:fixed` 讓多頁報告每頁右下角都會顯示。三語皆有對應標籤。(`33d1a6a`)

### P0（高優先）修復
- **草稿自動暫存**：表單文字/下拉輸入自動存 `localStorage`，斷線或送出失敗不會遺失已填內容；重新打開頁面會提示是否恢復草稿（照片不會被儲存，需重新選擇）。
- **照片上傳平行化＋重試**：改為 3 張併發上傳，每張失敗自動重試 2 次，單張失敗不再中斷整份送出。
- **翻譯失敗可見警示**：Gemini 翻譯失敗時，中文 PDF 會加上紅色警示 banner，LINE 給負責工程師的訊息標題與顏色改為警示樣式，email 也同步加上警示文字，不再靜默 fallback 使用原文。(`a15ae91`)

### P1（中優先）修復
- **語言自動偵測**：首次開啟表單依 LINE 使用者語系自動選擇泰/中/英文，不再永遠預設英文（使用者手動切換過的語言會被記住，不會被自動偵測覆蓋）。
- **選項載入失敗可見提示**：Google Sheets 選項 CSV 讀取失敗時顯示可見警示 banner，不再只是 console 靜默失敗。
- **送出前確認頁**：表單驗證通過後先顯示唯讀摘要（含照片張數），使用者按「確認送出」才會真正上傳；送出失敗按「重試」會回到確認頁而非整份重填。
- **天氣改為必填**：上午/下午天氣欄位加入必填驗證。(`a15ae91`)

---

*本檔案記錄面向使用者可見的功能變更；程式碼層級細節與部署狀態另見專案記憶（非公開）。*
